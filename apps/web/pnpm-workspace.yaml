# pnpm 11 decide con `allowBuilds` que dependencias pueden correr scripts
# de instalacion. esbuild necesita el suyo para dejar su binario nativo; sin
# esto `pnpm install` corta con ERR_PNPM_IGNORED_BUILDS.
allowBuilds:
  esbuild: true
